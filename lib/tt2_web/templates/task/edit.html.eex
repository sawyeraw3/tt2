<h1>Edit Task</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.task_path(@conn, :update, @task)) %>

<span><%= link "Tasks (don't save)", to: Routes.task_path(@conn, :index), class: "btn btn-primary" %></span>

